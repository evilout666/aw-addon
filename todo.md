#General
All buttons with name Channel ID need to be renamed to Channel, same for Category
all commands should have reset option like example [p]afterwork reset audio
#RSS Feed Setup
button: Remove Feed (Command) need to be renamed to Remove. button should be red because its remove function. instead of add feed should be just feed

#Radarr and Sonarr Setup
Target Channel rename to Set Channel

#Discord Embed Manager Setup
after its deployed by using [p]afterwork deploy it was never pimmed like the others was. 
in the embed mesage Status shows diferent icon and wrong text, should be Inactive and Active, toggle status button supposed to be named Disable and enable like the rest. Set Channel shouild be Channel
when i remove channel This interaction failed error, when i try again i get  No targets set up yet. because it removed, remove dropdown menu and have field because i can just write added name and press button remove, its safer , no accidental removal
Toggle status gave me error This interaction failed and didnt switch status
